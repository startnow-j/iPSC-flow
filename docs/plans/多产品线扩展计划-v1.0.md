# iPSC-Flow 多产品线扩展计划 v1.0

> **文档版本**：v1.2  
> **编制日期**：2026-06-18  
> **最后更新**：2026-06-22  
> **编制人**：系统设计（与用户讨论确认）  
> **状态**：Phase 2 实施中  
> **关联文档**：  
> - PRD v2.0（MVP 已完成）  
> - 多产品线生产实现方案（archive/多产品线生产实现方案-后续参考.md）  
> - 生产管理流程规划方案（archive/生产管理流程规划方案-早期素材.md）  
> - 双模式交互架构设计文档（docs/architecture/）

---

## 修订记录

| 版本 | 日期 | 变更内容 | 编制人 |
|------|------|---------|--------|
| v1.0 | 2026-06-18 | 初版：4阶段扩展计划，含关键设计决策 | 系统设计 |
| v1.1 | 2026-06-20 | Phase 2 详细设计更新：REJECTED处理、鉴定机制、交接、任务模板等用户确认的决策 | 系统设计 |
| v1.2 | 2026-06-20 | 新增 §2.6 用户权限体系设计：两层权限方案（全局角色+产品权限），含操作权限矩阵 | 系统设计 |
| v1.3 | 2026-06-20 | 新增 §2.7 导航架构设计：侧边栏按产品线分三个独立入口 | 系统设计 |

---

## 1. 背景与目标

### 1.1 现状

MVP v2.0 已完成，系统当前支持：
- iPSC 细胞产品线（扩增冻存流程）的完整闭环
- 单一产品（IPSC-WT-001），4种全局角色
- 11种批次状态、16条转换规则的硬编码状态机
- eBPR 三步流程（种子复苏→扩增培养→收获冻存）
- 质检录入、CoA 自动生成与审核放行
- Dashboard、用户管理、审计日志

### 1.2 核心差距

| # | 差距 | 影响 | 对应阶段 |
|---|------|------|---------|
| 1 | 无产品线概念，系统仅支持单一产品 | 无法管理服务项目、试剂盒等不同业务流 | Phase 1 |
| 2 | 角色全局化，无产品线维度 | 无法实现"员工A是X产品的生产、Y产品的质检" | Phase 1 |
| 3 | 状态机硬编码单套流程 | 其他产品线有完全不同的状态流转 | Phase 2 |
| 4 | eBPR 表单固定3步 iPSC 流程 | 服务项目需要8+步骤，试剂盒需要不同的生产表单 | Phase 3 |
| 5 | 质检模板仅支持 iPSC 3项 | 不同产品线的质检项目完全不同 | Phase 3 |
| 6 | 无步骤级执行人指定 | 主管无法将具体步骤分配给特定人员 | Phase 1 |

### 1.3 扩展目标

```
┌─────────────────────────────────────────────────────────────────┐
│                    扩展目标全景                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Phase 1: 产品线 + 产品基础架构                                   │
│  ─────────────────────────────────                               │
│  · 3条产品线（SERVICE / CELL_PRODUCT / KIT）                     │
│  · 每条产品线至少2个具体产品（共6+产品）                          │
│  · 用户-产品角色关联（用户+具体产品+角色）                        │
│  · 步骤级执行人指定                                              │
│                                                                 │
│  Phase 2: 多产品线状态机                                         │
│  ─────────────────────────────────                               │
│  · 3套流程模板（参数化状态转换规则）                               │
│  · 按产品线自动匹配对应流程模板                                   │
│  · 服务项目：订单→样本→生产→鉴定→报告→交付（含交接机制）          │
│  · 细胞产品：种子→生产→质检→CoA→放行（当前已实现，模板化）        │
│  · 试剂盒：指令→备料→配制→质检→CoA→放行                          │
│                                                                 │
│  Phase 3: UI 组件适配                                            │
│  ─────────────────────────────────                               │
│  · 服务项目表单（重编程/基因编辑/分化）                           │
│  · NPC 分化表单                                                  │
│  · 试剂盒配制/分装表单                                           │
│  · 各产品线质检模板                                              │
│  · CoA 模板差异化（服务项目=实验报告+CoA，试剂盒=简化版）         │
│                                                                 │
│  Phase 4: AI 对话模式                                            │
│  ─────────────────────────────────                               │
│  · 意图层实现（Intent Layer）                                    │
│  · LLM 集成（z-ai-web-dev-sdk）                                  │
│  · 确认卡片（Confirmation Card Protocol）                        │
│  · 上下文服务（Context Service）                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.4 验收标准

每阶段完成后必须达到：
- **3条产品线 × 各至少2个产品** 的全流程跑通
- 新建批次 → 生产记录 → 质检 → CoA → 放行的完整闭环

---

## 2. 关键设计决策

### 2.1 用户-产品关联粒度

| 方案 | 描述 | 优缺点 |
|------|------|--------|
| ~~A: 用户+产品线+角色~~ | 如"张三在细胞产品线是操作员" | 粒度粗，扩展性差 |
| **B: 用户+具体产品+角色** ✅ | 如"张三在IPSC-WT-001上是操作员，在NPC-001上是质检" | 粒度细，扩展性强 |

**决策**：选择方案 B（用户+具体产品+角色）

**理由**（用户确认）：
1. 更好的扩展性：未来产品增多时无需重构
2. 更符合实际业务：团队扩大后分工更专业，同一产品线的不同产品可能由不同人负责
3. 实现复杂度可接受：UserProductRole 关联表已在 schema 中定义

### 2.2 产品线与种子产品定义

| 产品线 | 枚举值 | 种子产品（Phase 1 创建） |
|--------|--------|------------------------|
| 服务项目 | `SERVICE` | 重编程建系服务(SRV-REPG-001)、基因编辑服务(SRV-EDIT-001) |
| 细胞产品 | `CELL_PRODUCT` | iPSC细胞株-野生型(IPSC-WT-001)【已有】、NPC神经前体细胞(NPC-001) |
| 试剂盒 | `KIT` | 神经分化试剂盒(KIT-ND-001)、iPSC维持培养基(KIT-MED-001) |

### 2.3 状态机参数化策略

| 产品线 | 状态流程 | 核心差异 |
|--------|---------|---------|
| 细胞产品 | NEW→IN_PRODUCTION→QC→CoA→RELEASED | 当前已实现，提取为模板 |
| 服务项目 | NEW→SAMPLE→IN_PRODUCTION→IDENTIFY→REPORT→DELIVERED | 含交接机制、鉴定融入生产、实验报告+CoA |
| 试剂盒 | NEW→PREP→IN_PRODUCTION→QC→CoA→RELEASED | 含物料准备阶段、效力验证(长周期) |

**策略**：3套流程模板常量，根据 `batch.productLine` 查找对应转换规则

### 2.4 批号格式

| 产品线 | 格式 | 示例 |
|--------|------|------|
| 服务项目 | `SRV-YYMMDD-NNN` | SRV-260618-001 |
| 细胞产品 | `{产品代码}-YYMMDD-NNN` | IPSC-260618-001 |
| 试剂盒 | `KIT-YYMMDD-NNN` | KIT-260618-001 |

### 2.5 AI 对话模式时机

**决策**：AI 对话模式放在 Phase 4（最后实施）

**理由**：
1. AI 模式需要依赖产品线上下文和流程模板才能正确工作
2. 前三个阶段完成后，AI 才有完整的"可操作空间"
3. 表单模式先跑通所有产品线流程，AI 模式作为增强叠加

### 2.6 用户权限体系设计

> **文档版本**：v2.0
> **状态**：已确认（QA/QC 分离 + 产品线归属 + 四眼原则），待实施
> **关联数据模型**：User（全局角色）、UserProductLine（产品线归属）、UserProductRole（产品级操作权限）

#### 2.6.1 设计原则

**QA/QC 分离，两级配置，四眼原则**

权限体系基于以下核心原则：

1. **角色分离**：QA（质量保证）是管理类角色，QC（质量控制）是操作类角色，两者职责完全不同
2. **两级配置**：ADMIN 管账号 + 全局角色 + 产品线归属；SUPERVISOR 管自己产品线内的具体产品操作权限
3. **双轨验证**：管理类操作看职级（全局角色 + 产品线归属），操作类操作看培训（全局资质 + 产品授权）
4. **四眼原则**：同一个人在同一个批次中不能既执行生产任务又执行质检任务

#### 2.6.2 角色体系：五个角色，两类性质

| 角色 | 枚举值 | 性质 | 职责定位 | 粒度 |
|------|--------|------|---------|------|
| 管理员 | `ADMIN` | **管理类** | 系统管理、用户管理 | 全局，天然全通 |
| 生产主管 | `SUPERVISOR` | **管理类** | 批次创建、CoA 审核、任务分配、产品权限配置 | 产品线级，需指定 |
| 质量保证 | `QA` | **管理类** | 审核体系、批准 SOP、审批偏差、终审 QC 结果 | 产品线级，需指定 |
| 质量控制 | `QC` | **操作类** | 执行质检实验（核型、流式、活力、菌检等） | 产品级，需具体授权 |
| 生产操作员 | `OPERATOR` | **操作类** | 执行生产任务（培养、冻存、传代等） | 产品级，需具体授权 |

**主角色优先级**：`ADMIN > SUPERVISOR > QA > QC > OPERATOR`

**QA vs QC 的本质区别**：

| | QA（质量保证） | QC（质量控制） |
|--|---------------|---------------|
| 性质 | 管理/监督 | 操作/执行 |
| 做什么 | 审核体系、批准 SOP、审批偏差 | 做实验——菌检、核型、流式、活力 |
| 工作场景 | 办公桌、会议室 | 实验台 |
| 需要产品级培训？ | ❌ 审流程合规性 | ✅ 不同产品检测方法不同 |

- 每个用户可以拥有多个全局角色（`User.roles` JSON数组）
- 一个用户可以同时拥有管理类 + 操作类角色（如 `["SUPERVISOR", "QC"]`）

#### 2.6.3 三级权限数据模型

```
第①层：User.roles = ["OPERATOR","QC"]              ← ADMIN 配置
       回答："张三有哪种资质？"

第②层：UserProductLine                              ← ADMIN 配置
       userId → 张三, productLine → CELL_PRODUCT
       回答："张三属于哪条产品线？活动范围是什么？"

第③层：UserProductRole (userId+productId+roles)     ← SUPERVISOR 配置
       userId → 张三, productId → IPSC-WT-001, roles → ["OPERATOR"]
       回答："张三能碰哪个产品？做哪种活？"
```

**数据表**：

| 表 | 配置者 | 存储 |
|---|--------|------|
| `User.roles` | ADMIN | 全局角色 JSON数组：`["OPERATOR","QC"]` |
| `UserProductLine` | ADMIN | 产品线归属：`userId + productLine` |
| `UserProductRole` | SUPERVISOR | 产品操作权限：`userId + productId + roles`（只存操作类角色） |

**粒度规则**（用户已确认）：
- ✅ **生产操作和质检执行**：粒度到具体产品（实验人员技能培训和工作分配到产品）
- ✅ **生产管理和质量保证**：粒度到产品线（SUPERVISOR/QA 管整条线，不需要逐产品分配）
- ✅ **管理员**：天然全通，不需要任何产品线/产品级分配

#### 2.6.4 权限判断逻辑（双轨验证）

废弃旧的"合并取并集"逻辑，改为管理类和操作类两条独立的检查路径：

```
用户能否执行操作？
│
├─ 如果是管理类操作（创建批次、审核CoA、分配任务、审批偏差...）
│   └─ hasGlobalRole(requiredRoles)                → 是/否
│   └─ isAdmin OR hasProductLine(productLine)      → 是/否（ADMIN 天然通过）
│
└─ 如果是操作类操作（执行生产、执行质检...）
    ├─ isAdmin                                     → 是 → 直接通过
    ├─ hasGlobalRole(requiredRoles)                → 否 → 拒绝（无此资质）
    └─ hasProductRole(productId, requiredRoles)    → 否 → 拒绝（该产品未授权）
                                                     是 → 通过
```

**代码表达**：

```typescript
// 管理类操作：全局角色 + 产品线归属
function canManage(user: User, productLine: string, requiredRoles: string[]): boolean {
  return isAdmin(user.roles)
      || (hasAnyRole(user.roles, requiredRoles)
          && hasProductLine(user.productLines, productLine))
}

// 操作类操作：全局资质 + 产品授权
function canOperate(user: User, productId: string, requiredRoles: string[]): boolean {
  return isAdmin(user.roles)
      || (hasAnyRole(user.roles, requiredRoles)
          && hasProductRole(user.productRoles, productId, requiredRoles))
}
```

#### 2.6.5 操作权限矩阵

| 操作 | 类型 | 全局角色要求 | 范围要求 | 判断函数 |
|------|------|------------|---------|---------|
| 查看批次列表/详情 | 查看 | 任意已登录 | ❌ | — |
| **执行生产任务** | 操作 | `OPERATOR` | ✅ 该产品有 `OPERATOR` 产品角色 | `canOperate()` |
| **执行质检** | 操作 | `QC` | ✅ 该产品有 `QC` 产品角色 | `canOperate()` |
| 创建批次 | 管理 | `SUPERVISOR`/`ADMIN` | ✅ 属于该产品线 | `canManage()` |
| 审核 CoA | 管理 | `SUPERVISOR`/`QA`/`ADMIN` | ✅ 属于该产品线 | `canManage()` |
| 分配任务 | 管理 | `SUPERVISOR`/`ADMIN` | ✅ 属于该产品线 | `canManage()` |
| 审批偏差/变更 | 管理 | `QA`/`SUPERVISOR`/`ADMIN` | ✅ 属于该产品线 | `canManage()` |
| 管理用户账号 | 管理 | `ADMIN` | ❌ 全局 | `canManage()` |
| 管理产品定义 | 管理 | `ADMIN`/`SUPERVISOR` | ✅ 属于该产品线 | `canManage()` |

#### 2.6.6 四眼原则（同批次生产/质检隔离）

> **规则：即使一个人在某个产品上同时拥有 OPERATOR 和 QC 权限，也不允许在同一个批次中既执行生产任务又执行质检任务。**

**拦截时机**：SUPERVISOR（或 ADMIN）指派任务时

```
指派 QC 任务到批次 A：
  → 查询：该人是否在批次 A 上已有 PRODUCTION 类型的已提交/进行中任务？
     → 是 → ❌ 拒绝，"已执行该批次的生产任务，不可同时执行质检"
     → 否 → ✅ 允许

指派 PRODUCTION 任务到批次 A：
  → 查询：该人是否在批次 A 上已有 QC 类型的已提交/进行中任务？
     → 是 → ❌ 拒绝，"已执行该批次的质检任务，不可同时执行生产"
     → 否 → ✅ 允许
```

**边界情况**：
| 场景 | 结果 |
|------|------|
| 张三做了批次 A 的生产，做批次 B 的质检 | ✅ 允许（不同批次） |
| 张三做了批次 A 的生产，做批次 A 的质检 | ❌ 拒绝 |
| ADMIN 指派绕过 | ❌ 同样受四眼原则约束 |

**实现层级**：
- **后端**：任务指派 API（`ProductionTask` / `QcRecord`）校验
- **前端**：指派人选择器中对冲突人员标记警告

#### 2.6.7 ADMIN 与 SUPERVISOR 配置职责

**职责总览**：

| 配置项 | 由谁做 | 在哪里做 | 说明 |
|--------|--------|---------|------|
| 创建用户账号 | ADMIN | 用户管理模块 | 基本信息（姓名、邮箱、密码、部门） |
| 分配全局角色 | ADMIN | 用户管理模块 | `User.roles`，定义资质类型 |
| 指定产品线归属 | ADMIN | 用户管理模块 | `UserProductLine`，决定活动范围 |
| 分配具体产品权限 | SUPERVISOR | 产品权限配置页面 | 自己产品线内，给成员分配 OPERATOR/QC |
| 查看权限全景 | ADMIN + SUPERVISOR + OPERATOR/QC | 权限总览页面 | ADMIN 全部，SUPERVISOR 自己线，操作类只看自己 |

**ADMIN 配置流程**（用户管理模块，创建/编辑用户对话框）：

```
┌──────────────────────────────────────────┐
│  创建用户                                │
│                                          │
│  姓名：[          ]                      │
│  邮箱：[          ]                      │
│  部门：[          ]                      │
│  密码：[          ]                      │
│                                          │
│  ─── 全局角色 ──────────────────────────  │
│  ☑ OPERATOR  ☑ QC  ☐ SUPERVISOR        │
│  ☐ ADMIN     ☐ QA                       │
│  → 定义张三的资质类型                     │
│                                          │
│  ─── 产品线归属 ──────────────────────── │
│  ☑ 服务项目(SERVICE)                     │
│  ☑ 细胞产品(CELL_PRODUCT)                │
│  ☐ 试剂盒(KIT)                           │
│  → 定义张三的活动范围                     │
│                                          │
│        [取消]  [创建]                     │
└──────────────────────────────────────────┘
```

**SUPERVISOR 配置流程**（产品权限配置页面，独立于用户管理）：

```
┌──────────────────────────────────────────────────┐
│  产品权限配置 — 细胞产品线                        │
├──────────────────────────────────────────────────┤
│                                                  │
│  选择用户：[张三 ▾]                               │
│                                                  │
│  ┌─────────────┬──────────┬────────┐             │
│  │ 产品         │ OPERATOR │  QC   │             │
│  ├─────────────┼──────────┼────────┤             │
│  │ IPSC-WT-001 │   ☑      │  ☐    │             │
│  │ NPC-001     │   ☐      │  ☑    │             │
│  │ MNC-001     │   ☑      │  ☑    │             │
│  └─────────────┴──────────┴────────┘             │
│                                                  │
│  ⚠️ 产品角色不能超出用户的全局资质                │
│  （全局无 QC 则不可勾选 QC）                      │
│                                                  │
│              [保存]                               │
└──────────────────────────────────────────────────┘
```

**约束校验**：
- SUPERVISOR 勾选的操作类角色不能超出用户的全局资质
- SUPERVISOR 只能操作自己产品线内的产品
- SUPERVISOR/QA 的产品线归属由 ADMIN 指定，指定后自动拥有该线管理权限

#### 2.6.8 权限总览页面

**路径**：`/settings/permissions`

**访问控制**：

| 角色 | 可见范围 |
|------|---------|
| ADMIN | 全部用户、全部产品线、全部产品权限分配 |
| SUPERVISOR | 仅自己产品线下的用户和产品权限分配 |
| OPERATOR / QC | 仅自己的权限分配（只读） |

**页面结构**：

```
┌─────────────────────────────────────────────────────┐
│  权限总览                                    [筛选▾] │
├─────────────────────────────────────────────────────┤
│                                                     │
│  产品线切换标签：[全部] [服务项目] [细胞产品] [试剂盒]  │
│                                                     │
│  ┌───────────────────────────────────────────────┐  │
│  │ 🔍 按姓名搜索...                              │  │
│  └───────────────────────────────────────────────┘  │
│                                                     │
│  ┌─────┬──────┬──────┬──────────────────────────┐  │
│  │ 姓名 │全局角色│产品线 │ 产品权限详情             │  │
│  ├─────┼──────┼──────┼──────────────────────────┤  │
│  │张三  │OP, QC│细胞  │ IPSC-WT-001: OPERATOR   │  │
│  │     │      │试剂盒│ NPC-001: QC              │  │
│  │     │      │      │ MNC-001: OP, QC          │  │
│  │     │      │      │ KIT-001: OPERATOR        │  │
│  ├─────┼──────┼──────┼──────────────────────────┤  │
│  │李主管│SUPER │细胞  │ （整线管理，无产品级分配） │  │
│  ├─────┼──────┼──────┼──────────────────────────┤  │
│  │王QA  │QA   │细胞  │ （整线质保，无产品级分配） │  │
│  └─────┴──────┴──────┴──────────────────────────┘  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

#### 2.6.9 任务指派 UX 优化（非权限控制，体验增强）

SUPERVISOR 指派任务时，UI 提供智能推荐：

- **指派操作员时**：推荐有该产品 `OPERATOR` 产品权限的人员（高亮/置顶），无权限人员淡色但仍可指派
- **指派质检员时**：推荐有该产品 `QC` 产品权限的人员
- **四眼原则标记**：对冲突人员（同批次已做另一类任务）显示警告图标
- **"我的任务"面板**：自动过滤，只显示当前用户有产品权限的待办任务

#### 2.6.10 数据模型变更记录

**v2.0 变更**（本版本）：

| 变更项 | 变更内容 | 原因 |
|--------|---------|------|
| `Role` 枚举 | 新增 `QC`（质量控制，操作类） | 区分 QA（管理）和 QC（操作） |
| `Role` 优先级 | `ADMIN > SUPERVISOR > QA > QC > OPERATOR` | QC 介于 QA 和 OPERATOR 之间 |
| 新增 `UserProductLine` 表 | `userId + productLine` | 产品线归属，ADMIN 配置 |
| `UserProductRole.roles` 语义 | 只存操作类角色（`OPERATOR`/`QC`） | 管理类角色不参与产品级分配 |
| `getProductRoles()` | 废弃合并逻辑，改为双轨验证 | 管理类和操作类走不同路径 |

#### 2.6.11 已有代码与待实施项

**已有（需适配）**：
- ✅ `UserProductRole` 数据模型（`prisma/schema.prisma`）— roles 语义更新为只存操作类角色
- ✅ `UserProductLine` 数据模型（`prisma/schema.prisma`）— 新增
- ⚠️ `getProductRoles()` 合并函数（`src/lib/roles.ts`）— 需重写为双轨验证
- ✅ 登录/认证接口返回 `productRoles`（`api/auth/login`、`api/auth/me`）— 需增加 `productLines`
- ✅ 前端 `useAuthStore` 中 `productRoles` 字段 — 需增加 `productLines`
- ✅ 用户列表中产品权限的展示（展开行）— 需适配 QC

**待实施**：
- ❌ 权限校验函数重写（`canManage()` + `canOperate()` 替代旧合并逻辑）
- ❌ 用户管理对话框增加全局角色 + 产品线归属配置区域
- ❌ 后端用户管理 API 支持产品线分配（`UserProductLine` CRUD）
- ❌ 产品权限配置页面（SUPERVISOR 在自己产品线内分配 OPERATOR/QC）
- ❌ 权限总览页面（ADMIN/SUPERVISOR/OPERATOR 分级查看）
- ❌ 四眼原则校验（任务指派时检查同批次生产/质检冲突）
- ❌ 后端任务执行、质检执行 API 增加产品权限校验
- ❌ 前端批次详情页任务/QC 操作按钮增加产品权限判断
- ❌ 前端"我的任务"面板增加产品权限过滤
- ❌ 前端任务指派人选择器增加推荐排序 + 四眼原则警告

### 2.7 导航架构设计

> **文档版本**：v1.0
> **状态**：已确认，待实施

#### 2.7.1 方案选型

| 方案 | 描述 | 结论 |
|------|------|------|
| A: 侧边栏按产品线分三个入口 | 每条产品线独立导航项，点击进入专属批次列表 | ✅ **采纳** |
| B: 单一入口 + 页内 Tab 切换 | 一个"批次管理"入口，页面内用 Tab 切换产品线 | ❌ 不采纳 |

**选择方案 A 的理由**：
1. 三条产品线的业务流程差异大——服务项目有"样本接收→交接→鉴定→报告"，试剂盒有"物料准备"，混在一起筛选器复杂
2. 实验人员通常只关注自己负责的产品线，不需要每次再点 Tab
3. Dashboard 已提供全局概览，批次列表不需要承担全量视图的职责
4. 各产品线入口可显示专属的状态筛选（如服务项目显示"交接中/鉴定中"，试剂盒显示"物料准备中"）

#### 2.7.2 侧边栏结构

```
侧边栏
├── 工作台                          /                (全局概览)
│
├── ── 生产管理 ──────────────────────────────────
├── 🧬 细胞产品                      /batches/cell-product
│   └── 批次列表（仅细胞产品线）
├── 🔬 服务项目                      /batches/service
│   └── 批次列表（仅服务项目线）
├── 🧪 试剂盒                        /batches/kit
│   └── 批次列表（仅试剂盒线）
│
├── ── 系统管理 ──────────────────────────────────
├── 全部批次                        /batches/all    (管理员/主管可见)
├── 产品管理                        /products
├── 用户管理                        /users
├── 审计日志                        /audit
```

#### 2.7.3 路由设计

| 路由 | 页面 | 说明 |
|------|------|------|
| `/batches/cell-product` | 细胞产品批次列表 | 自动锁定 `productLine=CELL_PRODUCT` |
| `/batches/service` | 服务项目批次列表 | 自动锁定 `productLine=SERVICE` |
| `/batches/kit` | 试剂盒批次列表 | 自动锁定 `productLine=KIT` |
| `/batches/all` | 全部批次列表 | 不锁定产品线，保留页内产品线切换（管理视角） |

#### 2.7.4 各产品线批次列表的差异

| 特性 | 细胞产品 | 服务项目 | 试剂盒 |
|------|---------|---------|--------|
| 状态筛选器 | 通用状态子集 | 含"交接中/鉴定中/待报告" | 含"物料准备中" |
| 列表卡片信息 | 批次号+状态+代次 | 批次号+状态+订单号 | 批次号+状态+产品名 |
| 新建批次对话框 | 现有逻辑（微调） | 含订单号+鉴定要求勾选 | 含物料准备阶段 |
| 批次图标 | FlaskConical | Microscope | TestTubes |

#### 2.7.5 实现要点

1. **路由层**：创建 `/batches/cell-product/page.tsx`、`/batches/service/page.tsx`、`/batches/kit/page.tsx`，均复用现有 `BatchListPage` 组件，通过 `defaultProductLine` prop 锁定
2. **侧边栏**：将现有"我的批次"+"所有批次"替换为三个产品线入口 + "全部批次"（移入系统管理区）
3. **状态筛选器**：`STATUS_FILTERS` 改为按产品线动态生成，各线只显示相关状态
4. **批次图标**：各产品线使用不同的 Lucide 图标区分

---

## 3. Phase 1 详细设计：产品线 + 产品基础架构

### 3.1 目标

- 数据库模型支持产品线和用户-产品-角色关联
- API 层支持按产品线过滤和权限控制
- 种子数据包含 3 条产品线的 6+ 个产品
- 步骤级执行人指定功能可用

### 3.2 数据库变更

#### 3.2.1 已完成的 Schema 变更（需验证）

以下字段已在 `prisma/schema.prisma` 中定义，需确认已同步到数据库：

```prisma
// Product 表新增
productLine    ProductLine @default(CELL_PRODUCT)  // ✅ 已有
category       String                               // ✅ 已有 (REPROGRAM/EDIT/IPSC/NPC/DIFF_KIT/MEDIUM)
cellType       String?                              // ✅ 已有

// Batch 表新增
productLine    ProductLine @default(CELL_PRODUCT)  // ✅ 已有
orderNo        String?                              // ✅ 已有

// UserProductRole 关联表  // ✅ 已有
model UserProductRole {
  id        String   @id @default(cuid())
  userId    String
  productId String
  roles     String   @default("[]")  // JSON数组
  @@unique([userId, productId])
}
```

#### 3.2.2 新增种子产品

```typescript
// prisma/seed.ts 新增
const SEED_PRODUCTS = [
  // === 细胞产品线 ===
  { productCode: 'IPSC-WT-001', productName: 'iPSC细胞株(野生型)', productLine: 'CELL_PRODUCT', category: 'IPSC', ... },
  { productCode: 'NPC-001',     productName: 'NPC神经前体细胞',    productLine: 'CELL_PRODUCT', category: 'NPC',  ... },

  // === 服务项目线 ===
  { productCode: 'SRV-REPG-001', productName: '重编程建系服务', productLine: 'SERVICE', category: 'REPROGRAM', ... },
  { productCode: 'SRV-EDIT-001', productName: '基因编辑服务',   productLine: 'SERVICE', category: 'EDIT',       ... },

  // === 试剂盒线 ===
  { productCode: 'KIT-ND-001',  productName: '神经分化试剂盒',  productLine: 'KIT', category: 'DIFF_KIT', ... },
  { productCode: 'KIT-MED-001', productName: 'iPSC维持培养基',  productLine: 'KIT', category: 'MEDIUM',    ... },
]
```

#### 3.2.3 新增种子用户-产品角色关联

```typescript
// 示例: supervisor 在所有产品上是 SUPERVISOR+OPERATOR
//       operator 在 IPSC-WT-001 上是 OPERATOR，在 NPC-001 上是 OPERATOR+QA
const SEED_USER_PRODUCT_ROLES = [
  { userId: adminId,       productId: 'IPSC-WT-001', roles: ['ADMIN'] },
  { userId: supervisorId,  productId: 'IPSC-WT-001', roles: ['SUPERVISOR', 'OPERATOR'] },
  { userId: operatorId,    productId: 'IPSC-WT-001', roles: ['OPERATOR'] },
  { userId: qaId,          productId: 'IPSC-WT-001', roles: ['QA'] },
  { userId: operatorId,    productId: 'NPC-001',     roles: ['OPERATOR', 'QA'] },
  // ... 更多产品关联
]
```

### 3.3 API 变更

#### 3.3.1 新增 API

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/product-roles` | GET | 获取当前用户的所有产品角色关联 |
| `/api/product-roles` | POST | 管理员设置用户-产品角色 |
| `/api/product-roles/[userId]/[productId]` | PATCH/DELETE | 修改/删除用户-产品角色 |
| `/api/batches` | GET | 新增 `productLine` 查询参数过滤 |
| `/api/products` | GET | 新增 `productLine` 查询参数过滤 |

#### 3.3.2 权限改造

当前权限检查使用全局 `roles` 数组（any-match）。Phase 1 需增加产品线维度：

```typescript
// 当前逻辑（全局角色）
if (!hasAnyRole(roles, ['SUPERVISOR', 'OPERATOR'])) return 403

// Phase 1 目标（产品级角色）
// 1. 获取用户在该产品上的角色
const userProductRoles = await getUserProductRoles(userId, batch.productId)
// 2. 检查是否满足要求
if (!hasAnyRole(userProductRoles, requiredRoles)) return 403
```

**过渡策略**：
- Phase 1 保持向后兼容：优先检查产品级角色，回退到全局角色
- 管理员（ADMIN）始终拥有所有权限

### 3.4 前端变更

#### 3.4.1 产品线导航与筛选

- 侧边栏新增产品线分组筛选
- 批次列表支持按产品线筛选
- 创建批次对话框按产品线分组展示产品选项
- Dashboard 统计按产品线分组

#### 3.4.2 产品线徽标组件

已有 `src/components/shared/product-line-badge.tsx`，需验证并完善：
- `SERVICE` → 紫色系
- `CELL_PRODUCT` → 绿色系（teal）
- `KIT` → 橙色系

#### 3.4.3 步骤级执行人指定

- 批次详情页显示各步骤的执行人
- 主管可将步骤分配给有权限的操作员
- 任务列表支持按执行人筛选

### 3.5 Phase 1 验收清单

- [ ] db:push 成功，Product/UserProductRole 表结构正确
- [ ] 种子数据运行成功，6+ 产品、UserProductRole 关联已创建
- [ ] /api/products 返回按产品线分组的产品列表
- [ ] /api/product-roles 返回当前用户的产品角色
- [ ] 创建批次时可选择不同产品线的不同产品
- [ ] 批号格式按产品线正确生成
- [ ] 批次列表支持产品线筛选
- [ ] 产品级角色权限检查生效（操作员只能操作被授权的产品）
- [ ] 步骤级执行人可在创建任务时指定

---

## 4. Phase 2 详细设计：多产品线状态机

### 4.1 目标

- 3套流程模板常量（按产品线分组）
- 参数化状态机：`getTransitions(productLine, status)` 替代硬编码 `BATCH_TRANSITIONS`
- BatchStatus 枚举扩展（一次性新增全部）
- 去掉 REJECTED 批次状态（CoA 表内保持 DRAFT/SUBMITTED/REJECTED）
- 任务模板骨架定义（Phase 3 实现表单 UI）
- 各产品线的状态流转全流程跑通

### 4.2 用户确认的设计决策（v1.1 更新）

#### 4.2.1 REJECTED 状态处理

**决策**：去掉 REJECTED 作为独立批次状态

| 场景 | 原方案 | 新方案 |
|------|--------|--------|
| 细胞产品 CoA 退回 | COA_SUBMITTED → REJECTED → COA_SUBMITTED | COA_SUBMITTED 状态下退回（CoA 表 status→DRAFT），批次状态不变 |
| 服务项目报告退回 | COA_SUBMITTED → REPORT_PENDING | COA_SUBMITTED → REPORT_PENDING（批次状态直接回退） |

**实现**：
- BatchStatus 枚举删除 REJECTED
- CoaStatus 枚举保持 DRAFT/SUBMITTED/REJECTED/APPROVED
- 细胞产品的 CoA 退回：只更新 CoA 表 status 为 DRAFT，批次保持 COA_SUBMITTED
- 服务项目的报告退回：批次状态 COA_SUBMITTED → REPORT_PENDING

#### 4.2.2 服务项目鉴定（IDENTIFICATION）机制

**决策**：IDENTIFICATION 作为批次状态（宏观阶段），内部子步骤通过 ProductionTask 管理（微观执行）

- 批次进入 IDENTIFICATION = 标志"重编程完成，进入鉴定阶段"
- 批次离开 IDENTIFICATION = 标志"所有鉴定项目完成"
- 鉴定子步骤（整合检测、核型、多能性、STR 等）都是 ProductionTask
- 鉴定任务没有严格 sequenceNo 排序，可分配给不同人员
- 鉴定完成由操作员/主管手动触发（因判定"全部完成"的逻辑灵活）
- 基因编辑的两轮克隆挑选：IDENTIFICATION → IN_PRODUCTION → IDENTIFICATION（状态机回退）

#### 4.2.3 交接机制（HANDOVER）

**决策**：允许多次交接，HANDOVER 作为轻量过渡状态

```
IN_PRODUCTION ⇄ HANDOVER（可循环多次）
```

- 每次交接创建一个 HANDOVER 类型的 ProductionTask
- 记录交接方、接收方、交接说明
- 接收确认后才能进入下一个状态
- 不限制交接次数

#### 4.2.4 鉴定不通过处理

**决策**：由主管判断如何处理

```
IDENTIFICATION
├── REPORT_PENDING    → 鉴定全部通过
├── IN_PRODUCTION     → 返工（主管决定）
└── SCRAPPED          → 报废（主管决定）
```

- 多能性质控不通过 → 可能只是实验问题 → IDENTIFICATION → IN_PRODUCTION
- 核型异常 → 该克隆不可救 → IDENTIFICATION → SCRAPPED
- 基因型不纯 → 换子克隆 → IDENTIFICATION → IN_PRODUCTION

#### 4.2.5 服务项目报告+CoA 流程

**决策**：简化流程，跳过独立 QC 环节

```
REPORT_PENDING → COA_SUBMITTED → RELEASED
                     ↑
                     └── REPORT_PENDING（退回修改）
```

- 跳过 QC_PASS → COA_PENDING → COA_SUBMITTED 链路
- 到报告环节时理论上所有鉴定/质检已通过
- 批准后直接放行（RELEASED）

#### 4.2.6 试剂盒效力验证

**决策**：必须完成后才能放行

- 效力验证作为可选质检项，创建批次时配置
- 如果指令单要求做效力检测 → 必须等完成 → 才能放行
- 效力检测 15-30 天的等待是正常业务过程（QC_IN_PROGRESS 状态持续）

#### 4.2.7 状态机模板结构

**决策**：方案A — 按产品线分组

```typescript
const TRANSITION_TEMPLATES: Record<ProductLine, Record<string, TransitionRule[]>> {
  CELL_PRODUCT: { /* 独立完整流程 */ },
  SERVICE:       { /* 独立完整流程 */ },
  KIT:           { /* 独立完整流程 */ },
}
```

**理由**：三条产品线差异远大于共性，分组方案可读性、维护性、安全性均优于扁平混合方案。

#### 4.2.8 任务模板策略

**决策**：Phase 2 定义任务骨架 + Phase 3 实现表单

- Phase 2：定义每个状态转换时创建哪些任务（taskCode/taskName/sequenceNo）
- Phase 3：为每种任务类型开发具体表单组件
- 鉴定要求在创建批次时由主管勾选，存入 Batch.identificationRequirements（JSON）
- 主管后续可追加/修改鉴定要求（动态创建新鉴定任务）

### 4.3 BatchStatus 枚举（最终版）

```prisma
enum BatchStatus {
  // === 通用状态 ===
  NEW              // 新建
  IN_PRODUCTION    // 生产中
  QC_PENDING       // 待质检
  QC_IN_PROGRESS   // 质检中
  QC_PASS          // 质检合格
  QC_FAIL          // 质检不合格
  COA_PENDING      // 待生成CoA
  COA_SUBMITTED    // CoA已提交审核
  COA_APPROVED     // CoA已批准
  RELEASED         // 已放行
  SCRAPPED         // 已报废

  // === 服务项目特有 ===
  SAMPLE_RECEIVED  // 样本已接收
  HANDOVER         // 交接中
  IDENTIFICATION   // 鉴定中
  REPORT_PENDING   // 待生成报告

  // === 试剂盒特有 ===
  MATERIAL_PREP    // 物料准备中
}
```

### 4.4 状态标签与颜色

| 状态 | 中文标签 | 颜色方案 | 适用产品线 |
|------|---------|---------|-----------|
| NEW | 新建 | gray | 通用 |
| IN_PRODUCTION | 生产中 | blue | 通用 |
| QC_PENDING | 待质检 | yellow | 通用 |
| QC_IN_PROGRESS | 质检中 | orange | 通用 |
| QC_PASS | 质检合格 | emerald | 通用 |
| QC_FAIL | 质检不合格 | red | 通用 |
| COA_PENDING | 待生成CoA | violet | 通用 |
| COA_SUBMITTED | CoA已提交 | sky | 通用 |
| COA_APPROVED | CoA已批准 | teal | 通用 |
| RELEASED | 已放行 | green | 通用 |
| SCRAPPED | 已报废 | stone | 通用 |
| SAMPLE_RECEIVED | 样本已接收 | blue | SERVICE |
| HANDOVER | 交接中 | amber | SERVICE |
| IDENTIFICATION | 鉴定中 | indigo | SERVICE |
| REPORT_PENDING | 待生成报告 | purple | SERVICE |
| MATERIAL_PREP | 物料准备中 | cyan | KIT |

### 4.5 三套完整流程模板

#### 4.5.1 细胞产品（CELL_PRODUCT）

```
NEW → IN_PRODUCTION → QC_PENDING → QC_IN_PROGRESS → QC_PASS → COA_PENDING(自动)
  → COA_SUBMITTED → COA_APPROVED → RELEASED
                                                             ↓
                                                     (CoA退回,CoA→DRAFT)
                                                             ↓
                                                     COA_SUBMITTED(批次不变)
                                                             ↓
                                                        COA_SUBMITTED → COA_APPROVED

QC_FAIL → QC_PENDING(返工) / SCRAPPED(报废)
任意非终态 → SCRAPPED(报废)
```

#### 4.5.2 服务项目（SERVICE）

```
NEW → SAMPLE_RECEIVED → IN_PRODUCTION ⇄ HANDOVER(可多次)
                              │
                              ↓
                        IDENTIFICATION ←─── IN_PRODUCTION(返工)
                         ╱    │    ╲
                        ↓     ↓     ↓
                  REPORT   IN_PROD  SCRAPPED
                   _PENDING
                      │
                      ↓
                COA_SUBMITTED ←─── REPORT_PENDING(退回修改)
                      │
                      ↓
                   RELEASED

任意非终态 → SCRAPPED(报废)
```

#### 4.5.3 试剂盒（KIT）

```
NEW → MATERIAL_PREP → IN_PRODUCTION → QC_PENDING → QC_IN_PROGRESS
  → QC_PASS → COA_PENDING(自动) → COA_SUBMITTED → COA_APPROVED → RELEASED

QC_FAIL → QC_PENDING(返工) / SCRAPPED(报废)
任意非终态 → SCRAPPED(报废)
```

### 4.6 任务模板定义

```typescript
const TASK_TEMPLATES: Record<ProductLine, Record<string, TaskDef[]>> = {
  CELL_PRODUCT: {
    start_production: [
      { taskCode: 'SEED_PREP',  taskName: '种子复苏',  sequenceNo: 1 },
      { taskCode: 'EXPANSION', taskName: '扩增培养',  sequenceNo: 2 },
      { taskCode: 'HARVEST',   taskName: '收获冻存',  sequenceNo: 3 },
    ],
  },
  SERVICE: {
    start_production: [
      { taskCode: 'REPROGRAM', taskName: '重编程操作', sequenceNo: 1 },
      // 鉴定任务根据 batch.identificationRequirements 动态创建
    ],
    start_identification: [
      // 激活之前创建的鉴定任务（PENDING → IN_PROGRESS）
    ],
    // 基因编辑服务:
    // start_production: [GENE_EDIT, CLONE_PICK_AND_IDENTIFY, EXPANSION, HARVEST]
  },
  KIT: {
    start_material_prep: [
      { taskCode: 'MATERIAL_PREP', taskName: '物料准备', sequenceNo: 1 },
    ],
    start_production: [
      { taskCode: 'PREPARATION', taskName: '配制生产', sequenceNo: 2 },
      { taskCode: 'DISPENSING',  taskName: '分装贴标', sequenceNo: 3 },
    ],
  },
}
```

### 4.7 Batch 表新增字段

```prisma
model Batch {
  // ... 现有字段 ...

  // 鉴定/质检要求配置（JSON数组，服务项目和试剂盒使用）
  // 示例: ["INTEGRATION", "KARYOTYPE", "PLURI_CHECK", "STR", "POTENCY"]
  identificationRequirements  String  @default("[]")  // JSON
}
```

### 4.8 Phase 2 验收清单

- [ ] BatchStatus 枚举更新（新增5个状态，去掉 REJECTED）
- [ ] db:push 成功同步数据库
- [ ] 3套 TRANSITION_TEMPLATES 常量定义完成
- [ ] `transition()` 函数按 `batch.productLine` 查找对应模板
- [ ] 细胞产品线：现有流程不受影响（回归验证）
- [ ] 细胞产品 CoA 退回逻辑改为 CoA 表内处理
- [ ] 服务项目线：NEW→SAMPLE_RECEIVED→IN_PRODUCTION→IDENTIFICATION→REPORT_PENDING→COA_SUBMITTED→RELEASED
- [ ] 服务项目：HANDOVER 多次交接、鉴定返工、报告退回
- [ ] 试剂盒线：NEW→MATERIAL_PREP→IN_PRODUCTION→QC→CoA→RELEASED
- [ ] 任务模板按产品线和 action 正确创建任务骨架
- [ ] 前端操作按钮根据产品线动态显示
- [ ] 批次详情页时间线正确记录各状态变更
- [ ] 种子数据包含 6+ 产品和示例批次

---

## 5. Phase 3 详细设计：UI 组件适配

### 5.1 目标

- 每条产品线有对应的生产记录表单
- 质检模板按产品线差异化
- CoA 模板按产品线差异化
- 全流程表单驱动跑通

### 5.2 各产品线 eBPR 表单

| 产品线 | 步骤 | 表单组件 | 关键字段 |
|--------|------|---------|---------|
| **服务项目-重编程** | 样本接收 | SampleReceiveForm | 样本信息、来源、存储位置 |
| | 重编程 | ReprogramForm | 导入因子、培养条件、克隆观察 |
| | 早代冻存/交接 | HandoverForm | 冻存信息、交接双方确认 |
| | 扩增培养 | ServiceExpansionForm | 传代记录（同细胞产品） |
| | 建系鉴定 | CharacterizationForm | 支原体/核型/多能性/STR |
| | 报告生成 | ReportForm | 实验报告+CoA合并 |
| **服务项目-基因编辑** | 样本接收 | SampleReceiveForm | 同上 |
| | 基因编辑 | GeneEditForm | sgRNA、电转条件、筛选方式 |
| | 克隆挑选 | ClonePickForm | 克隆编号、筛选结果 |
| | 鉴定 | GeneEditIdentificationForm | PCR/测序验证 |
| **细胞产品-NPC** | 种子复苏 | SeedPrepForm | 复用现有 |
| | 分化培养 | DifferentiationForm | 分化方案、每日观察、标志物检测 |
| | 收获冻存 | HarvestForm | 复用现有（调整规格） |
| **试剂盒** | 物料准备 | MaterialPrepForm | 原料核对、批号记录 |
| | 配制生产 | PreparationForm | 多组分配制、过滤除菌 |
| | 分装贴标 | DispensingForm | 分装数量、贴标信息 |

### 5.3 质检模板差异化

| 产品线 | 必检项 | 选检项 | 差异点 |
|--------|--------|--------|--------|
| 细胞产品(iPSC) | 复苏活率≥85%、形态正常、支原体阴性 | 核型、基因型 | 当前已实现 |
| 细胞产品(NPC) | 复苏活率≥80%、支原体阴性、标志物表达(流式) | 纯度检测 | 新增标志物检测 |
| 服务项目 | 融入鉴定过程，不做独立质检 | — | 鉴定结果即质检 |
| 试剂盒 | 无菌检测(必检) | 效力验证(委托细胞产品组) | 效力验证为长周期(15-30天) |

### 5.4 CoA 模板差异化

| 产品线 | CoA 内容 | 特点 |
|--------|---------|------|
| 细胞产品 | 产品信息+质检结果+原始图片 | 完整CoA，当前已实现 |
| 服务项目 | 实验报告+CoA合并 | 包含完整建系过程记录 |
| 试剂盒 | 产品信息+无菌检测结果 | 简化CoA，无原始图片 |

### 5.5 Phase 3 验收清单

- [ ] 服务项目（重编程/基因编辑）全流程表单可用
- [ ] NPC 分化表单可用
- [ ] 试剂盒配制/分装表单可用
- [ ] 各产品线质检模板正确展示
- [ ] 各产品线 CoA 模板正确展示
- [ ] 3条产品线 × 各2个产品的全流程跑通

---

## 6. Phase 4 详细设计：AI 对话模式

### 6.1 目标

- 意图层（Intent Layer）实现
- LLM 集成（后端 z-ai-web-dev-sdk）
- 确认卡片（Confirmation Card Protocol）
- 上下文服务（Context Service）
- 对话工作台 UI

### 6.2 依赖关系

Phase 4 必须在 Phase 1-3 完成后实施：
- 意图层依赖完整的产品线和流程模板定义
- 上下文服务依赖所有产品线的业务数据
- 确认卡片依赖所有表单的字段定义和 ai_hint

### 6.3 核心模块

```
Phase 4 模块分解：
├── 后端
│   ├── 意图层 (Intent Layer)
│   │   ├── 意图定义（基于已有 PRD 中的 operation_intent 设计）
│   │   ├── 意图解析器（LLM 提取意图+参数）
│   │   └── 意图执行器（调用对应业务服务）
│   ├── 上下文服务 (Context Service)
│   │   ├── 批次上下文（当前状态、历史记录、下一步建议）
│   │   └── 默认值服务（培养基批号、操作人等自动填充）
│   ├── 确认卡片服务 (Card Protocol)
│   │   ├── 卡片构建器（根据意图和参数生成卡片数据）
│   │   └── 卡片渲染协议（前端通用渲染器）
│   └── 对话 API
│       ├── POST /api/conversation — 发送消息
│       ├── GET /api/conversation/[id] — 获取对话历史
│       └── POST /api/conversation/[id]/confirm — 确认卡片
│
└── 前端
    ├── 对话工作台组件
    │   ├── 聊天窗口（消息列表、输入框）
    │   ├── 确认卡片渲染器（通用组件）
    │   └── 模式切换（对话/表单）
    └── 对话 Store（Zustand）
```

### 6.4 Phase 4 验收清单

- [ ] 对话工作台 UI 可用（消息列表、输入框、确认卡片）
- [ ] 能通过自然语言完成 iPSC 传代记录录入
- [ ] AI 提取数据→确认卡片→用户确认→写入数据库 完整流程
- [ ] 上下文感知（AI 知道当前批次状态和历史）
- [ ] 表单/对话模式无缝切换
- [ ] 审计日志区分 FORM_SUBMIT / AI_CONVERSATION

---

## 7. 实施优先级与依赖关系

```
Phase 1 ────┐
(DB+API)    │
            ├──→ Phase 3 (UI适配)
Phase 2 ────┘     │
(状态机)           │
                   └──→ Phase 4 (AI对话)
```

- **Phase 1 和 Phase 2 可并行开发**
- **Phase 3 依赖 Phase 1 + Phase 2**
- **Phase 4 依赖 Phase 1 + Phase 2 + Phase 3**

---

## 8. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 状态机改造影响现有流程 | 细胞产品线当前可用的功能回退 | Phase 2 先确保现有流程不受影响，再逐步添加新模板 |
| 产品级权限检查过于复杂 | 开发周期延长 | Phase 1 向后兼容，渐进式改造 |
| 6+产品的表单开发量大 | Phase 3 周期过长 | 优先实现最关键的表单，次要表单用通用表单替代 |
| SQLite 不支持某些高级查询 | 多产品线联查性能问题 | 当前数据量小，SQLite 足够；后续可迁移 |
